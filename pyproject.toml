[project]
name = "production-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi",
    "langchain-anthropic",
    "langgraph",
    "langsmith",
    "pydantic-settings",
    "python-dotenv",
    "slowapi",
    "uvicorn",
]
