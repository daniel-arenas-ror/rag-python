[project]
name = "production-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "deepagents>=0.7.9",
    "fastapi",
    "langchain>=1.3.17",
    "langchain-anthropic",
    "langchain-openai>=1.6.0",
    "langgraph",
    "langsmith",
    "pydantic-settings",
    "python-dotenv",
    "slowapi",
    "uvicorn",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.1.1",
]
